import React, { Component } from 'react';
import { Link, withRouter } from 'react-router-dom';

import Navbar from './Navbar';

export default class SurveyType extends Component {
    render(){
        return(<div>Hello</div>);
    }
}

// export default SurveyType;
